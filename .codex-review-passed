story: P0-1 — technician earnings must count cash jobs
branch: fix/p0-1-earnings-truth
base: main (post #315 + #316)
reviewer: codex-cli 0.147.0
rounds: 2

round 1: P2 — buildEarningEvents ignored cashCollectedAmount, so once P0-2 ships
         the client field, earnings could report money never collected. Fixed in
         the round-2 commit, which also clamps net at zero: an unclamped negative
         would 500 the dashboard, whose schema declares todayEarningsInPaise
         nonnegative and is parsed.
round 2: clean — "type-safe, covered by targeted tests, no discrete correctness
         issue introduced by the diff."

gate: tools/pre-codex-smoke-api.sh PASSED (typecheck + lint + 1561 tests / 188 files)

note: suite is intermittently flaky under parallel load on this machine even with
      hookTimeout raised to 30s; failures are "Hook timed out", pass in isolation,
      and differ run to run. Pre-existing, unrelated to this change.
