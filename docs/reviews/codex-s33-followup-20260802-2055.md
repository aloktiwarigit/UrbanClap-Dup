# Codex review — S-33 follow-up (Paparazzi golden sequencing fix)

**Date:** 2026-08-02 20:55 EDT
**Branch:** feat/s33-states
**Scope:** docs/reviews/s33-followup.diff — follow-up commits d7a53ebe, 245dc741, 9d801030 on top of
the original PR #303 diff (which already passed its own Codex round: 0 CRITICAL / 0 MAJOR / 1 MINOR
accepted).

## What changed since the original PR #303 review

1. Removed class-level `@Ignore` from 4 Paparazzi test classes (ShieldReportSheet/RatingAppealSheet,
   en+hi) — they were never executing, so the first `paparazzi-record.yml` run produced zero images
   for them.
2. Extracted `ShieldReportSheetContent`/`RatingAppealSheetContent` composables out of their
   `ModalBottomSheet` wrappers — Paparazzi's single-frame `snapshot{}` never lets
   `ModalBottomSheet`'s entrance animation settle, so the first (correctly un-ignored) recording
   produced 6 byte-identical blank goldens.
3. Committed the 6 resulting golden PNGs after individually inspecting each for correct content,
   locale, and enabled/disabled button state.

## Codex findings (STATIC ONLY, no build)

**CRITICAL**
None.

**MAJOR**
- `PendingActionCardPaparazziTest.kt:21` — still class-level `@Ignore`. Combined with the
  `PendingActionCardKt` Kover exclusion added earlier in this PR, the actual ticking-countdown
  Compose behavior (the bug PR #303 fixes) is neither screenshot-verified nor covered by
  `koverVerify` — only the extracted pure `remainingSeconds` math is unit-tested. **This is about
  the original PR #303 scope (PendingActionCard fix), not the follow-up commits in this round** —
  it was not touched by this session's golden-sequencing fix. Flagging for owner: was this
  intentionally left out of golden-recording scope (per SESSION-STATE.md, only the 4 Shield/
  RatingAppeal tests were specified), or should PendingActionCardPaparazziTest also be recorded now?

**MINOR**
- `ShieldReportSheetPaparazziTest.kt:27`, `RatingAppealSheetPaparazziTest.kt:27` — new snapshots test
  `*SheetContent` only, not the `ModalBottomSheet` wrapper. Correct call to dodge Paparazzi's blank-
  animated-sheet issue, but test names/docs still read as covering `ShieldReportSheet`/
  `RatingAppealSheet` — drag handle, sheet shape/insets, and dismiss wiring are NOT covered by these
  goldens. Consider renaming for clarity in a future pass.

## Verdict

Composable extraction is behaviorally sound: state moved cleanly into the extracted content, submit
gating/normalization unchanged, wrapper still owns `ModalBottomSheet` state/dismiss handling. No
CRITICAL or MAJOR findings within this round's actual diff. The one MAJOR is a pre-existing gap in
PR #303's original scope, surfaced by this round's review but not introduced by it.

**Network note:** first review attempt failed after 5 retries with a TLS certificate mismatch
connecting to `chatgpt.com` (presented cert was for `*.solutionip.com` — looked like local VPN/proxy
TLS interception). Second attempt succeeded without any config change. If this recurs, check VPN/
proxy state before assuming Codex itself is down.
