# Codex review — issue #334 (service commissionBps clearable + roster)

**Reviewed commit:** `6cfbf387` (base: `main`)
**Verdict:** Pass — no CRITICAL or MAJOR findings.

## Findings (both P3/Minor, both fixed in follow-up commit `2f2a2a78`)

1. **Stale `servicesError` banner survives a successful retry** — `handleClearService`
   never reset `servicesError` on a new attempt, so a resolved failure kept
   displaying as broken. Fixed: clear the error state at the start of every attempt.

2. **Reused category-level "Inherit global" label on a service row** — a service
   falls back to its category's rate before global, so the label misstated the
   actual outcome whenever the service's category also carried an override.
   Fixed: dedicated `settings.rates.clearOverrideService` key ("Inherit
   category/global") in both `en.json` and `hi.json`. Confirms a defect the
   Task 7 task-reviewer had already independently found and parked as a
   deferred-minor in the SDD ledger.

Not re-reviewed by Codex after the fix — both changes are mechanical, low-risk,
and directly implement what Codex itself specified. See `.codex-review-passed`
for the marker.
