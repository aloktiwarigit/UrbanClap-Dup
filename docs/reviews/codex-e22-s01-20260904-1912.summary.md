# Codex review — E22-S01 round 1 (against main 90aef6ff, head ed885dd8)

```
Full review comments:

- [P2] Reject price text in Hindi descriptions — C:\Alok\Business Projects\wt-e22-s01\api\src\schemas\service.ts:39-39
  Because the new admin form sends `shortDescriptionHi` through this schema, an ops user can save `₹599`/`Rs`/`INR` in Hindi copy; the public API returns it and `CatalogueLocalizer` prefers server Hindi, so a later price edit can recreate the stale-price defect this change is trying to remove. The seed CI check does not protect admin-created or admin-updated services, so enforce the no-price-prose rule at this new persisted field as well.

- [P2] Check each Kotlin Hindi fallback map — C:\Alok\Business Projects\wt-e22-s01\tools\check-hindi-catalogue-parity.mjs:36-38
  This only checks whether the service id appears anywhere in `HindiLocaleNames.kt`. If a future service is added to `serviceHindiNames` but not `serviceShortDescriptionsHindi`, or if the description fallback keeps an old rupee-containing string while the seed changes, CI still passes even though old APKs will render English or stale price prose from the fallback. Parse and validate both Kotlin maps, including the same price-free text check.
The main implementation compiles and targeted tests pass, but the new Hindi description field can still store stale price prose through admin updates, and the CI parity guard does not fully validate the Kotlin fallback it is meant to protect.

Full review comments:

- [P2] Reject price text in Hindi descriptions — C:\Alok\Business Projects\wt-e22-s01\api\src\schemas\service.ts:39-39
  Because the new admin form sends `shortDescriptionHi` through this schema, an ops user can save `₹599`/`Rs`/`INR` in Hindi copy; the public API returns it and `CatalogueLocalizer` prefers server Hindi, so a later price edit can recreate the stale-price defect this change is trying to remove. The seed CI check does not protect admin-created or admin-updated services, so enforce the no-price-prose rule at this new persisted field as well.

```

Full transcript: codex-e22-s01-20260904-1912.md.transcript.md
