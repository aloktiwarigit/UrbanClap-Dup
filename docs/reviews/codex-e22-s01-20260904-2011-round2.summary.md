# Codex review — E22-S01 round 2 (against main 90aef6ff, head ab06223c)

Round-1 findings (schema accepts price text; guard checks id presence only) were NOT re-raised — resolved by 792d22e9 and ab06223c.

```
Full review comments:

- [P2] Recognize Hindi currency words in prose guard — C:\Alok\Business Projects\wt-e22-s01\api\src\schemas\service.ts:12-12
  For Hindi copy such as `सिर्फ 599 रुपये में` or `599 रु.`, this regex does not match any price marker, so the new Hindi fields can still store price literals and later display stale prices in the Hindi-default customer app. Please include common Hindi currency words/abbreviations in the guard, not just `₹`, `Rs`, and `INR`.

- [P2] Apply no-price validation to nested prose fields — C:\Alok\Business Projects\wt-e22-s01\api\src\schemas\service.ts:68-71
  When an admin/API client creates or updates a service with price text in `includes`, `faq`, `addOns`, or `photoStages`, these fields still accept strings like `₹250/m` because the new `noPriceInProse` refinement is only applied to names/descriptions. These fields are served in `ServiceDetailSchema`, so the same stale-price problem can still be introduced through the service body; derive these nested string schemas from the same guarded validator.
The patch leaves gaps in the new no-price-in-prose invariant, allowing stale prices through Hindi currency wording and nested service prose fields. Typechecks and focused tests pass, but these validation gaps should be fixed.

```

Disposition: both P2s fixed in the follow-up commit (Hindi currency words in the regex; refinement applied to includes/faq/addOns/photoStages). No third Codex round per the two-round cap — see .codex-review-passed.

Full transcript: codex-e22-s01-20260904-2011-round2.transcript.md
