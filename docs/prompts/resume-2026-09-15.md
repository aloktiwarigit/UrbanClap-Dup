# Resume — 2026-09-15 (session handoff)

Previous session's context window filled up (orchestrated 3 parallel peer sessions through E24-S01/E22-S02/E21-S05, then did REL-1 release prep). This doc hands off a **live, in-progress investigation** plus some cheap cleanup. Read this fully before doing anything.

## What's DONE (do not redo)

- **E24-S01 (UPI/QR), E22-S02 (catalogue), E21-S05 (technician KYC/wallet)** — all merged to `main`. Full BMAD ceremony + Codex + CI passed on each.
- **CI infra**: `e2e-and-a11y` required-check-shadowing bug (PR #354) found and fixed (PR #356) — no-op jobs in customer/technician/api/design-system/docs-ship.yml now correctly gate on a `dorny/paths-filter` `admin-relevance` job so they don't shadow the real admin-web check.
- **Docs**: `docs/patterns/paparazzi-cross-os-goldens.md` corrected twice (PR #357, #358).
- **REL-1 release**:
  - PR #360 (versionCode bump) — **merged**. Real values now on `main`: customer-app `versionCode=13`/`versionName=0.1.8`, technician-app `versionCode=15`/`versionName=0.1.12`. These safely exceed the **real** last-uploaded Play Console values the owner confirmed directly: customer=12, technician=14 (a stale branch had wrongly suggested 14/15 as current — do not trust that; `main` is the source of truth now).
  - PR #361 (`tools/build-play-bundles.ps1` UTF-8 BOM fix for PowerShell 5.1 parse bug) — **merged via admin override** (no CI workflow path covers `tools/*.ps1`, so `quality-gate`/`e2e-and-a11y` never fire on it — that's expected, not a bug. Codex verified the diff was byte-for-byte BOM-only before merge.)
  - Both release AABs **built and verified**: `wt-rel-1/customer-app/app/build/outputs/bundle/release/app-release.aab` (45.3MB) and `wt-rel-1/technician-app/app/build/outputs/bundle/release/app-release.aab` (15.7MB), signed with the real upload keystore, built with `ALLOW_BLANK_OBSERVABILITY_KEYS=true` per owner's explicit "ship blank for now" answer (Sentry/PostHog/GrowthBook keys are a pre-existing known gap, not a new one — see `project_sentry_dsn_empty.md`).
  - **Installed on the owner's Moto G** via wireless ADB (paired + connected at `192.168.1.80:36277`) using `bundletool` (downloaded to `~/AppData/Local/Temp/claude/bundletool/bundletool.jar`, official Google OSS tool) to convert AABs → installable APK sets. Old debug-signed installs were uninstalled first (owner explicitly approved the data-loss tradeoff). Both apps launch cleanly, no crashes in logcat, on-device versionCode/versionName confirmed matching (13/0.1.8 and 15/0.1.12).

## What's IN PROGRESS — pick this up first

Owner asked: **"why homehero still shows 5 services"** on the freshly-installed customer-app.

Answered (confirmed against `origin/main:api/src/cosmos/seeds/catalogue.ts`): there are exactly 5 `isActive: true` categories in prod right now — AC Repair, Water Pump/Borewell, Plumbing, Electrical, RO/Water Purifier. Everything from E22-S02 (new "Appliance Repair" category: fridge repair, cooler service, washing-machine repair; `electrical-camera-installation`; an Inverter service) was seeded `isActive: false` on purpose, gated by `api/tests/scripts/seed-technicians.test.ts`'s launch-gate assertion: every active category/service needs ≥2 online, KYC-approved technicians covering it (`tech.skills.includes(svc.id)`).

Owner said **"yes"** to: *"Want me to check current technician coverage counts for those categories to see how close we are to [onboarding techs and flipping isActive:true]?"*

**This check was interrupted mid-flight.** State when cut off:
- `api/scripts/seed-technicians.ts` is **not** real prod data — it's a fixed 10-technician Ayodhya pilot fixture used for local/test seeding only. None of its hardcoded `skills` cover the 5 new inactive services (all its skills map to the 5 already-active categories).
- `api/local.settings.json` has **no** `COSMOS_ENDPOINT`/`COSMOS_KEY` set locally — can't query prod Cosmos directly from a local script without pulling credentials first.
- Azure CLI **is** logged in (`az account show` → subscription "Azure subscription 1", user `aloktiwari0630@yahoo.com`), so the plan was: pull `COSMOS_CONNECTION_STRING` (or endpoint+key) from the `func-homeservices-prod` Function App's app settings (`az functionapp config appsettings list --name func-homeservices-prod --resource-group rg-homeservices-prod`), then run a **read-only** one-off script against the real `technicians` container in prod Cosmos to count, per new service/category, how many real technicians have matching skills AND are online AND KYC-approved.
- Relevant prior finding already in memory (`project_technician_kyc_never_worked.md`): as of the E21-S05 investigation, prod had **16 real technicians, 0 with any kyc object** (kyc had never worked end-to-end before E21-S05 shipped the fix). E21-S05 is now merged, so this may have changed — don't assume it's still 0, re-check.

**Next step**: finish the coverage query (read-only against prod Cosmos), report real counts per new service/category to the owner, then let the owner decide: onboard technicians to cover the gap, or explicitly accept shipping a category without confirmed coverage (that would need an explicit owner override of the launch-gate rationale, not a silent flip).

## Minor cleanup backlog (low priority, do opportunistically)

Stale worktrees still on disk for already-merged branches — safe to `git worktree remove --force` + delete the local branch once you confirm nothing uncommitted is in them:
- `wt-335-a11y-quarantine`, `wt-api36`, `wt-e21-s05`, `wt-e22-s02`, `wt-e24-s01`, `wt-e24-s01c`, `wt-fix-ci-required-check`, `wt-fix-paparazzi-doc`, `wt-fix-paparazzi-doc2`, `wt-rel-1`

Main working directory (`fix/customer-privacy-policy-url` branch, never merged — has stale local edits) has uncommitted changes unrelated to this session's work (`.gitignore`, `.serena/project.yml`, `admin-web/lighthouserc.cjs` modified; several untracked files including a WhatsApp export zip/folder with the price notes used earlier, `docs/play-store-listing-technician.md`, `docs/uat-handoff-plan.md`). Don't touch these blind — they may be another in-progress thread. Ask the owner before cleaning.

## Standing facts to carry over (don't re-derive)

- Real Play Console last-uploaded versionCode: customer=12, technician=14 (owner-confirmed, 2026-09-15).
- `.codex-review-passed` is a rolling marker file on `main`, rewritten by every merging PR — always read its `commit` field, never trust its mere existence.
- Never run Paparazzi `recordPaparazziDebug` on Windows — cross-OS font rendering breaks goldens. Use `paparazzi-record.yml` workflow_dispatch on CI with explicit `gradle_root`/`gradle_task` (both silently default to customer-app if left blank).
- `tools/build-play-bundles.ps1` requires the real `google-services.json` (not the SEC-01 stub) and the real signing keystore + `local.properties` per app — none of this is available in CI, this script is local-only by design.
- bundletool jar cached at `~/AppData/Local/Temp/claude/bundletool/bundletool.jar` (v1.18.1) if more device installs are needed this cycle.
